# Decoder-powerful-tool-for-decode-txt
This is a small tool for repairing and decoding potentially garbled TXT files that need to decode.  characteristic: - Automatically detect file encoding (using `charset-normalizer` or `chardet`） - Try to fix common mojibake (garbled characters caused by double encoding) - Output as UTF-8 encoded text file
